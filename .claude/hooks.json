{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -X POST http://localhost:8787/api/hooks/session-start -H 'Content-Type: application/json' -d @- || true",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -X POST http://localhost:8787/api/hooks/user-prompt-submit -H 'Content-Type: application/json' -d @- || true",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -X POST http://localhost:8787/api/hooks/pre-tool-use -H 'Content-Type: application/json' -d @- || true",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -X POST http://localhost:8787/api/hooks/stop -H 'Content-Type: application/json' -d @- || true",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
